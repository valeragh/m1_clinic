<div class="container" style="min-height:750px;">
  <div class="row">
    <div class="col-sm-6 col-md-4 col-md-offset-4">
      <h1 class="text-center">Регистрация</h1>
      <p class="text-center">Заполните поля для регистрации</p>
      <div class="account-wall">
        <%= image_tag "add_user_icon.png", class: "profile-img" %>

        <%= form_for(resource, as: resource_name, url: registration_path(resource_name), :html => {:class => "form-signin"}) do |f| %>

          <%= f.text_field :name, class: "form-control", placeholder: "Имя/Логин" %>
          <%= errors_for @user, :name %>
          <%= f.email_field :email, autofocus: true, class: "form-control", placeholder: "Email" %>
          <%= errors_for @user, :email %>
          <%= f.password_field :password, autocomplete: "off", class: "form-control", placeholder: "Пароль минимум 8 символов" %>
          <%= errors_for @user, :password %>
          <%= f.password_field :password_confirmation, autocomplete: "off", class: "form-control", placeholder: "Повторите пароль" %>
          <%= errors_for @user, :password_confirmation %>
          <%= f.submit "Регистрация", class: "btn btn-lg btn-primary btn-block" %>
          <%= render "devise/shared/links" %>
        <% end %>
      </div>
    </div>
  </div>
</div>


