.row
  .col-sm-12.procedures_show{:style => "background-image: url(#{image_path "#{@service_category.image_url}"})"}

  .col-sm-12
    .forum-header
      %h1
        = @service_category.name
      %p
        Вступительный текст о услугах и процедурах Первой Мужской Клиники

  - @services.each do |service|
    .col-sm-4
      .profile-content.doctor
        = link_to procedure_path(@service_category[:slug], service[:slug]) do
          .img{:style => "background-image: url(#{image_path "#{service.image_small_url}"})"}
        .profile-content-copy
          %p.category
            = @service_category.name
          = link_to procedure_path(@service_category[:slug], service[:slug]) do
            %h3.service.profile-name
              = service.name
          %p.profile-summary
            Узнайте больше
          = link_to procedure_path(@service_category[:slug], service[:slug]) do
            %i.fa.fa-long-arrow-right.fa-3x
  .col-sm-12
    .forum-header
      %p
        = link_to procedures_path do
          %i.fa.fa-long-arrow-left.fa-3x
          %p
            Все услуги
= render "layouts/doctor_online"
.row
  = render "layouts/instagram"
